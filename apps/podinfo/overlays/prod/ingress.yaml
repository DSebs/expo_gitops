apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: springboot-ingress
  namespace: prod
spec:
  rules:
    - host: springapp-prod.local
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: springapp
                port:
                  number: 80
